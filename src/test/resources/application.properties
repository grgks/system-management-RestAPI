spring.datasource.url=jdbc:mysql://localhost:3306/appointment_system_restdb_test?serverTimezone=Europe/Athens
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.time_zone=Europe/Athens
spring.jackson.time-zone=Europe/Athens

jwt.secret=${JWT_SECRET}
jwt.expiration=7200000

logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
